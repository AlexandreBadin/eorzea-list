<!-- <div class="navbar navbar-expand-sm navbar-light navbar-lewagon">
  <%= link_to (root_path), class: "navbar-brand" do %>
    <h1>E<span style="color: red">O</span>RZEA LIST</h1>
  <% end %>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  
  <%= link_to lists_path, class: 'btn btn-info' do %>
    <i class="fas fa-list"></i>
    My Lists
  <% end %>
  
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <% if user_signed_in? %>
          <li class="nav-item dropdown">
            <%= image_tag current_user.avatar_url, class: "avatar dropdown-toggle", id: "navbarDropdown", data: { toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false %>
            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
              <%= link_to "Account", "#", class: "dropdown-item" %>
              <%= link_to "Disconnect", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
            </div>
          </li>
          <li class="nav-item active">
            <span class="nav-link"><%= current_user.username %>#<%= current_user.discriminator %></span>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to 'Sign in with Discord', user_discord_omniauth_authorize_path, method: :post, class: 'btn btn-primary' %>
          </li>
        <% end %>
      </ul>
    </div>
</div> -->


<div class="navbar">
  <h1>E<span style="color: red">O</span>RZEA LIST</h1>

  <%= link_to lists_path, class: 'lists-btn' do %>
    <i class="fas fa-list"></i>
    My Lists
  <% end %>

  <div class="user-content">
    <% if user_signed_in? %>
      <%= image_tag current_user.avatar_url, id: "user-avatar" %>
      <span class="username-discriminator"><%= current_user.username %>#<%= current_user.discriminator %></span>
    <% else %>
      <%= link_to 'Sign in with Discord', user_discord_omniauth_authorize_path, method: :post, class: 'discord-btn' %>
    <% end %>
  </div>
</div>